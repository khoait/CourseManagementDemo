/* eslint-disable*/
import { vus_studentMetadata } from "./entities/vus_student";

export const Entities = {
  vus_student: "vus_student",
};

// Setup Metadata
// Usage: setMetadataCache(metadataCache);
export const metadataCache = {
  entities: {
    vus_student: vus_studentMetadata,
  },
  actions: {
  }
};